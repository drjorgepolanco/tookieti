- if @user.errors.any?
  #error_explanation
    .alert-box.alert.radius
      El formulario contiene #{pluralize(@user.errors.count, 'error')}.
    %ul
      = @user.errors.full_messages.each do |msg|
        %li= msg

