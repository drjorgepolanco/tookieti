- provide(:title, full_name(@user))

= render "show_user"

- if @user.recipes.any?
  .row
    .large-12.small-12.columns
      .pagination-centered
        = will_paginate @recipes, renderer: FoundationPagination::Rails, inner_window: 1, outer_window: 0
      %br/

      = render "shared/list", obj: @recipes

      .pagination-centered
        = will_paginate @recipes, renderer: FoundationPagination::Rails, inner_window: 1, outer_window: 0
      %br/